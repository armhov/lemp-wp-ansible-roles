# lemp-wp-ansible-roles
for run playbook
1. in hosts file change the remote server ip/hostName
2. change the aws public key file path/name
3. change the remote server ip in nginx config file (wordpress.conf)
